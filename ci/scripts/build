#!/bin/bash

VERSION="$(cat version/number)"

export GOPATH=$GOPATH:$(pwd)
cd src/github.com/cloudcredo/graphite-nozzle
export GOPATH=$GOPATH:$(pwd)/Godeps/_workspace

go build -o graphite-nozzle main.go
tar -cvzf "graphite-nozzle-$VERSION.tar.gz" ./graphite-nozzle
